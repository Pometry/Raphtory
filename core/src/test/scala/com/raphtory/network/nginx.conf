worker_processes auto;

error_log /var/log/nginx/error.log info;

events {
    worker_connections  1024;
}

stream {
    upstream backend {
        server host.docker.internal:<OUTPUT_PORT>;
    }

    server {
        listen <INPUT_PORT>;
        proxy_pass backend;
    }
}